// function createBubbles() {
//   const joinSection = document.createElement('section')
//   const joinSpan = document.createElement('span')
//   let size = Math.random() * 60
//
//   joinSpan.style.width = size + 'px'
//   joinSpan.style.height = size + 'px'
//   joinSpan.style.left = Math.random() * window.innerWidth + 'px';
//
//   joinSection.appendChild(joinSpan)
  // return <span
  // width={size + 'px'}
  // height={size + 'px'}
  // left={Math.random() * window.innerWidth + 'px'}
  // >
  // </span>
// }

// setInterval(createBubbles(), 50)
